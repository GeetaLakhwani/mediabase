
server:
  port: 8080

spring:
  application:
    name: moviefun

  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp

logging.level:
  root: WARN

  org.superbiz.moviefun.podcastsUI: DEBUG

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000


#movies.ms.url: http://localhost:8081/movies
#podcast.ms.url: http://localhost:8082/podcasts
