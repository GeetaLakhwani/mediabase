server.port: 8090

spring:
  jpa:
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

    application:
      name: podcasts-ms

  datasource:
    url: jdbc:mysql://localhost:3306/podcasts?useSSL=false
    username: root